
-- Baseline migration for existing production database
-- This migration is intentionally empty and marks the starting point
-- All tables are created using IF NOT EXISTS clauses in subsequent migrations

-- No-op: Database baseline established
SELECT 1;
